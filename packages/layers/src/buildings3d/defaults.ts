/**
 * Default values for 3D buildings layers.
 *
 * @module buildings3d/defaults
 */

/**
 * Default minimum zoom level for 3D buildings (13 is a good balance).
 */
export const DEFAULT_MIN_ZOOM = 13;

/**
 * Default fill-extrusion color (light gray).
 */
export const DEFAULT_COLOR = "#aaaaaa";

/**
 * Default fill-extrusion opacity (0.8 for good visibility with some transparency).
 */
export const DEFAULT_OPACITY = 0.8;

/**
 * Default height when property is missing (10 meters).
 */
export const DEFAULT_HEIGHT = 10;
